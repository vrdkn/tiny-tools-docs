# tiny-tools-docs

This is a test README for the practical task.  
Here will be the documentation of the project.

## Installation

```bash
git clone ...
cd tiny-tools-docs
python -m venv venv
source venv/bin/activate  # or venv\Scripts\activate on Windows
pip install -r requirements.txt
